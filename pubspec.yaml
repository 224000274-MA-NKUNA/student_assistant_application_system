name: student_assistant_app
description: Student Assistant Application System
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  supabase_flutter: ^2.8.0
  image_picker: ^1.0.7
  file_picker: ^8.0.0
  intl: ^0.18.1
  url_launcher: ^6.2.5
  cross_file: ^0.3.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
 
  assets:
    - assets/images/